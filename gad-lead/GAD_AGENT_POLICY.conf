# GAD Agent Preference v1
#
# Only four role preferences are needed.
# Missing / empty / default / system / auto / unsupported / disabled / unavailable
# values fall back to Orca's configured Default Agent.
#
# Existing live Lead / Worker sessions are never switched automatically.
# New Lead sessions and newly-created Worker sessions use the latest preferences.

lead = claude
implementation = codex
review = claude
other = default
